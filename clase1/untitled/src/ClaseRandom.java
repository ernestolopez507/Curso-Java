import java.util.Random;

public class ClaseRandom {
    //utiliza el metodo random de la clase Match 
    //es mucho mas flexible para generar numeros enteros y decimales aleatorios
    Random  randomObj = new Random();
    int randomInt = randomObj.nextInt(0);
}
